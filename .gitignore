*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
.cursor
.history
.kotlin
.monkeycode/

# Local release signing material (keystore + passwords), never commit
signature/

# Firebase config is supplied per-build, never commit it
app/google-services.json